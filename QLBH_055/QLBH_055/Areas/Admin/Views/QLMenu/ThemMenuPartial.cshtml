@using QLBH_055.Models
@model MENU





<div class="col-md-12 text-center">
    <div class="card card-plain">
        <div class="header">
            <h2>Thêm Menu</h2>

        </div>
        <div class="content table-responsive table-full-width">
            @using (Html.BeginForm("ThemMenu", "QLMenu", FormMethod.Post))
            {
                
                        <div class="layout">
                            @Html.TextBoxFor(n => n.TENMN, new { @class = "textboxchung", @placeholder = "Tên menu" })
                        </div>
                        <div class="layout">
                            @Html.TextBoxFor(n => n.HREF, new { @class = "textboxchung", @placeholder = "Đường dẫn" })
                        </div>
                        <div class="layout">
                            <input style="font-size:18px;" id="btnthem" type="submit" class="btn btn-info" value="Thêm" />
                        </div>
                  
            }
        </div>
    </div>
</div>
