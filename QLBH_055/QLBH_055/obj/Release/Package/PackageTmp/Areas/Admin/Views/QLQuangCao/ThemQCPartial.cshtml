
@using QLBH_055.Models
@model QUANGCAO



<div class="col-md-12 text-center">
    <div class="card card-plain">
        <div class="header">
            <h2>Thêm quảng cáo</h2>

        </div>
        <div class="content table-responsive table-full-width">
            @using (Html.BeginForm("ThemQuangCao", "QLQuangCao", FormMethod.Post))
            {
           
                        <div class="layout">
                            @Html.TextBoxFor(n => n.TENQC, new { @class = "textboxchung", @placeholder = "Tên quảng cáo" })
                        </div>
                        <div class="layout">
                            <input name="file" type="file" placeholder="Ảnh" />
                        </div>
                        <div class="layout">
                            @Html.TextBoxFor(n => n.HREF, new { @class = "textboxchung", @placeholder = "Đường dẫn" })
                        </div>
                        <div class="layout">
                            @Html.TextBoxFor(n => n.THUTUQC, new { @class = "textboxchung", @placeholder = "Thứ tự" })
                        </div>
                        <div class="layout">
                            <input style="font-size:18px;" id="btnthem" type="submit" class="btn btn-info" value="Thêm" />
                        </div>
                
            }
        </div>
    </div>
</div>
